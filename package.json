{
  "name": "larry_token",
  "scripts": {
    "test": "npx hardhat test",
    "start": "npx hardhat compile",
    "deploy" : "npx hardhat run scripts/deploy.js --network customNetwork"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "dotenv": "^16.0.3",
    "hardhat": "^2.14.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.3"
  }
}
